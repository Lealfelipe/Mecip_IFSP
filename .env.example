# Exemplo de variáveis de ambiente para Mecip_IFSP
# Ajuste conforme necessário

# Django
# Gere uma nova chave com: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-sua-chave-secura-aqui
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (exemplo sqlite - não necessário alterar para sqlite)
# Para Postgres, troque por: ENGINE=django.db.backends.postgresql, NAME, USER, PASSWORD, HOST, PORT
DATABASE_URL=sqlite:///db.sqlite3

# Email (exemplo para console backend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Outros
# MEDIA_ROOT, STATIC_ROOT podem ser configurados aqui se necessário
